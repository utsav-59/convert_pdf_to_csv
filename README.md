# PDF to CSV Converter
Convert Bank Statement's PDF to CSV.

**Limitation**
Support's only HDFC Bank statement.

Steps:
* Run "docker build -t <docker_image_name> <path_of_folder>"
* Run "docker run <docker_image_name>"
